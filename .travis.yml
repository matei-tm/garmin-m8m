language: java

jdk: oraclejdk11

script:
    - cd m8m && ./travis.sh

deploy:
    provider: pages
    skip-cleanup: true
    local_dir: public
    github-token: $GITHUB_TOKEN
    keep-history: true
    target_branch: gh-pages
    on:
        branch: master
